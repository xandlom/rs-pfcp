# v0.3.0 Release Plan - Breaking Changes

**Created:** 2025-12-14
**Last Updated:** 2026-02-08
**Target Release:** Q1 2026
**Type:** MAJOR - Breaking Changes
**Status:** Implementation Complete - Ready for Release

---

## ✅ **STATUS UPDATE (2026-02-08)**

**ALL v0.3.0 FEATURES ARE 100% COMPLETE!**

All three breaking changes are fully implemented and tested. The release is ready for final review and publishing.

### 🎉 What's Complete:

**PfcpError Migration (100% DONE):**
- ✅ PfcpError enum with 9 variants (src/error.rs)
- ✅ 100% IE, message, and builder layer coverage
- ✅ 3GPP Cause code mapping via `to_cause_code()`
- ✅ Temporary bridge conversion removed

**Newtype Wrappers (100% DONE):**
- ✅ `src/types.rs` with `Seid`, `SequenceNumber`, `Teid`
- ✅ Message trait updated (`seid() -> Option<Seid>`, `sequence() -> SequenceNumber`)
- ✅ Header, Fseid, Fteid updated to use newtypes
- ✅ All builders accept `impl Into<Type>` for ergonomic construction
- ✅ All examples updated

**Deprecated Method Removal (100% DONE):**
- ✅ `find_ie()` and `find_all_ies()` removed from Message trait
- ✅ All call sites migrated to `ies()` iterator API

**Documentation (100% DONE):**
- ✅ Migration guide: `docs/guides/v0.3.0-migration.md`
- ✅ CHANGELOG.md v0.3.0 section written
- ✅ All architecture and guide docs updated for v0.3.0 API
- ✅ README.md version bumped to 0.3.0

### 📊 Next Step:

Bump `Cargo.toml` version and publish.

---

## 📋 Executive Summary

v0.3.0 is the **first major breaking release** since v0.2.0, delivering **type safety** and **structured error handling**. All development work is complete.

**Goals (ALL ACHIEVED):**

1. ✅ **PfcpError migration** - COMPLETE (done in v0.2.x)
2. ✅ **Add newtype wrappers** for type-safe primitives (Seid, SequenceNumber, Teid)
3. ✅ **Remove deprecated methods** from v0.2.x (find_ie, find_all_ies)
4. ✅ Maintain 100% test coverage
5. ✅ Comprehensive migration guide

**Status:** Ready for release (pending version bump and benchmarks)

---

## 🎯 Core Features

### 1. Custom Error Type (`PfcpError`) - ✅ **100% COMPLETE**

**Status:** Fully implemented in v0.2.x series (completed 2026-02-04)

**What's Done:**
- ✅ PfcpError enum with 8 variants (src/error.rs, 1,369 lines)
- ✅ 100% IE layer - all unmarshal methods migrated
- ✅ 100% message layer - all 25 message types migrated
- ✅ 100% builder layer - all builders use PfcpError
- ✅ Core infrastructure - Ie::unmarshal, IeIterator, accessor methods
- ✅ Comparison module - semantic.rs, builder.rs, compare.rs
- ✅ 3GPP Cause code mapping (to_cause_code() method)
- ✅ Rich error context with structured variants
- ✅ 2,081 tests passing
- ✅ Zero std::io::Error returns in IE/message/builder layers

**Documentation:** See `docs/analysis/completed/custom-error-type.md` for full implementation history.

**No remaining work for v0.3.0** - this feature is complete!

---

### Original Feature Description (Mostly Implemented):

**Current Problem:**

```rust
// ❌ All errors are generic io::Error
pub fn unmarshal(data: &[u8]) -> Result<Self, io::Error> {
    Err(io::Error::new(io::ErrorKind::InvalidData, "Missing PDR ID"))
}

// ❌ Users must parse error strings (brittle!)
match msg.unmarshal(data) {
    Err(e) if e.to_string().contains("Missing PDR ID") => { /* ... */ }
}
```

**Proposed Solution:**

```rust
// ✅ Structured error type with rich context
pub enum PfcpError {
    /// IE parsing errors
    IeParseError {
        ie_type: IeType,
        reason: String,
        offset: Option<usize>,
    },

    /// Missing mandatory IE
    MissingMandatoryIe {
        ie_type: IeType,
        message_type: Option<MsgType>,
    },

    /// Invalid field value
    InvalidValue {
        field: String,
        expected: String,
        got: String,
    },

    /// Validation errors (builders)
    ValidationError {
        message: String,
    },

    /// UTF-8 encoding errors
    EncodingError {
        source: std::str::Utf8Error,
    },

    /// Underlying I/O errors
    IoError {
        source: io::Error,
    },
}

impl std::error::Error for PfcpError { /* ... */ }
impl Display for PfcpError { /* ... */ }
impl From<io::Error> for PfcpError { /* ... */ }
impl From<std::str::Utf8Error> for PfcpError { /* ... */ }
```

**Benefits:**

- ✅ **Pattern matching** on error types
- ✅ **Error recovery** - handle specific cases
- ✅ **Better debugging** - structured context
- ✅ **3GPP mapping** - can map to PFCP Cause codes
- ✅ **Error chains** - preserve error context

**Implementation Leverage:**

- ✅ v0.2.4 already has error message constants in `src/error.rs`
- ✅ Just need to add enum and migrate call sites
- ✅ Message constants directly map to error variants

**Effort:** 3-4 days
**Files Affected:** ~150 files (all marshal/unmarshal implementations)
**Breaking:** Yes (changes all `Result<T, io::Error>` → `Result<T, PfcpError>`)

---

---

### 2. Newtype Wrappers - ✅ COMPLETE

**Current Problem:**

```rust
// ❌ Easy to swap arguments (both are primitives)
SessionEstablishmentRequest::new(1234, 5678);  // Which is SEID? Which is sequence?
SessionEstablishmentRequest::new(5678, 1234);  // Compiles but wrong!

// ❌ No semantic meaning
let seid: u64 = 0x123456789ABCDEF0;
let teid: u32 = 0x12345678;
let seq: u32 = 42;
// All primitives - easy to mix up
```

**Proposed Solution:**

```rust
// ✅ Type-safe newtypes
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub struct Seid(pub u64);

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub struct SequenceNumber(pub u32);

#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub struct Teid(pub u32);

impl Seid {
    pub const fn new(value: u64) -> Self { Self(value) }
    pub const fn value(&self) -> u64 { self.0 }
}

// ✅ Compiler prevents swapping
SessionEstablishmentRequest::new(
    Seid::new(0x123456789ABCDEF0),
    SequenceNumber::new(42)
);  // Type-safe!

SessionEstablishmentRequest::new(
    SequenceNumber::new(42),  // ❌ Compile error!
    Seid::new(0x123456789ABCDEF0)
);
```

**Types to Wrap:**

| Type | Inner | Priority | Usage |
|------|-------|----------|-------|
| `Seid` | `u64` | HIGH | Session Endpoint ID - used in 12+ messages |
| `SequenceNumber` | `u32` | HIGH | Message sequence - used in all messages |
| `Teid` | `u32` | HIGH | Tunnel Endpoint ID - F-TEID IE |
| `Priority` | `u8` | MEDIUM | QoS priority values |

**Note:** `Precedence` already exists as a proper type! ✅

**Benefits:**

- ✅ **Compile-time safety** - prevent argument swapping
- ✅ **Self-documenting** - clear semantic meaning
- ✅ **Validation** - can add constructors that validate ranges
- ✅ **Domain logic** - can add impl blocks for conversions

**Effort:** 1-2 days
**Files Affected:** ~40 files (message constructors, builders)
**Breaking:** Yes (changes constructor signatures)

---

### 3. Remove Deprecated Methods - LOW PRIORITY

**Cleanup deprecated APIs from v0.2.x:**

```rust
// These deprecated methods have been REMOVED:
// - Message::find_ie()         // Replaced by: ies(ie_type).next()
// - Message::find_all_ies()    // Replaced by: ies(ie_type).collect()
```

**Deprecation Timeline:**

- v0.2.2 (2025-12-05): Methods marked `#[deprecated]`
- v0.2.3-v0.2.5: Deprecation warnings guide users
- v0.3.0: Remove deprecated methods entirely

**Effort:** 0.5 days (straightforward removal)
**Files Affected:** ~30 files (message types + trait)
**Breaking:** Yes (removes deprecated methods)

---

## 📅 Implementation Plan

### Phase 0: Custom Error Type - ✅ COMPLETE

**All tasks completed in v0.2.x series (2026-01-25 through 2026-02-04):**

- [x] PfcpError enum designed and implemented (src/error.rs)
- [x] Display, Error, From implementations
- [x] All IE files migrated (100+ files)
- [x] All message files migrated (25 message types)
- [x] All builders migrated
- [x] Core infrastructure migrated (Ie::unmarshal, IeIterator)
- [x] Comparison module migrated
- [x] Tests updated and passing (2,081 tests)
- [x] Documentation updated

**See:** `docs/analysis/completed/custom-error-type.md` for full history.

---

### Phase 1: Newtype Wrappers - ✅ COMPLETE

**Task 2.1: Create Newtype Definitions** ✅

- [x] Create `src/types.rs` with newtype wrappers
- [x] Add `Seid`, `SequenceNumber`, `Teid`
- [x] Implement std traits (Debug, Clone, Eq, Ord, Hash)
- [x] Add const constructors, accessors, Deref, From impls
- [x] Add comprehensive tests

**Task 2.2: Migrate Message Constructors** ✅

- [x] Update all 25 message type constructors
- [x] Update message builders
- [x] Update `Message` trait (seid, sequence, set_sequence)
- [x] All tests passing

**Task 2.3: Migrate IE Constructors** ✅

- [x] Update F-TEID (uses Teid)
- [x] Update F-SEID (uses Seid)
- [x] Header updated to use Seid and SequenceNumber

**Task 2.4: Update Examples** ✅

- [x] Update all examples with new types
- [x] All examples compile and run

---

### Phase 3: Cleanup & Polish - ✅ COMPLETE

**Task 3.1: Remove Deprecated Methods** ✅

- [x] Remove `find_ie()` and `find_all_ies()`
- [x] Update any remaining call sites
- [x] Full test suite passing

**Task 3.2: Documentation** ✅

- [x] Write migration guide (`docs/guides/v0.3.0-migration.md`)
- [x] Update CHANGELOG.md with v0.3.0 section
- [x] Update README with version bump
- [x] Update all architecture and guide docs for v0.3.0 API

**Task 3.3: Testing & Validation** ✅

- [x] Full test suite passing
- [x] All examples compile
- [x] Clean clippy and cargo check

**Task 3.4: Community Review**

- [ ] Create v0.3.0-rc.1 release candidate
- [ ] Announce in discussions/issues
- [ ] Gather feedback

---

## 📊 Effort Summary

| Phase | Task | Effort | Risk | Status |
|-------|------|--------|------|--------|
| **Phase 0** | Custom Error Type (PfcpError) | - | - | **✅ DONE in v0.2.x** |
| **Phase 1** | Newtype Wrappers (Seid, Seq, Teid) | 2-3 days | LOW | **✅ DONE** |
| **Phase 2** | Remove Deprecated Methods | 0.5-1 day | LOW | **✅ DONE** |
| **Phase 3** | Documentation & Migration Guide | 1-2 days | LOW | **✅ DONE** |
| **Community** | Review Period (RC releases) | 2-4 weeks | LOW | Pending |
| **Total** | Development | **4-6 days** | **LOW** | **✅ Complete** |

**Note:** PfcpError migration was completed ahead of schedule, significantly reducing v0.3.0 scope!

**Calendar Timeline:**

- Week 1: Phase 1 (Newtype Wrappers)
- Week 1-2: Phase 2 (Remove Deprecated Methods)
- Week 2: Phase 3 (Documentation & Migration Guide)
- Week 3-6: Community review & feedback (RC releases)
- Week 6-7: Final release

**Target Release:** 6-8 weeks from start (Q1-Q2 2026)

---

## 🎯 Success Criteria

### Must Have ✅

- [x] PfcpError migration complete
- [x] Newtype wrappers implemented and tested
- [x] All tests passing
- [x] Zero clippy warnings
- [x] All examples compile and run
- [x] Migration guide is complete and tested
- [x] CHANGELOG documents all breaking changes
- [ ] Performance: No regressions (benchmarks within 5%)

### Should Have 📋

- [ ] Community feedback incorporated
- [ ] At least 2 weeks of rc testing
- [ ] Migration tested on real projects
- [ ] Error messages are helpful and consistent

### Nice to Have 🌟

- [ ] Error type examples in docs/guides
- [ ] YouTube video explaining migration
- [ ] Blog post about type safety improvements

---

## ⚠️ Risks & Mitigation

### Risk 1: Breaking Changes Impact Users
**Likelihood:** HIGH
**Impact:** MEDIUM
**Mitigation:**

- ✅ Provide comprehensive migration guide
- ✅ Release candidate period for feedback
- ✅ Clear deprecation timeline (v0.2.2 → v0.3.0)
- ✅ Document all breaking changes in CHANGELOG

### Risk 2: Error Type Complexity
**Likelihood:** MEDIUM
**Impact:** MEDIUM
**Mitigation:**

- ✅ Leverage existing error message constants from v0.2.4
- ✅ Start with simple enum, can expand later
- ✅ Provide From<io::Error> for backward compat in some places

### Risk 3: Performance Regression
**Likelihood:** LOW
**Impact:** HIGH
**Mitigation:**

- ✅ Newtype wrappers are zero-cost
- ✅ Error type uses enums (efficient)
- ✅ Run benchmarks before/after
- ✅ Only accept if performance within 5%

### Risk 4: Community Resistance
**Likelihood:** LOW
**Impact:** MEDIUM
**Mitigation:**

- ✅ Clear communication about benefits
- ✅ Migration guide with examples
- ✅ RC period for feedback
- ✅ Be willing to adjust based on feedback

---

## 📦 Migration Guide Preview

### For Users of rs-pfcp

**Updating Error Handling:**

```rust
// Before (v0.2.x)
match msg.unmarshal(data) {
    Ok(msg) => process(msg),
    Err(e) => eprintln!("Error: {}", e),
}

// After (v0.3.0) - Option 1: Simple migration
match msg.unmarshal(data) {
    Ok(msg) => process(msg),
    Err(e) => eprintln!("Error: {}", e),  // Still works!
}

// After (v0.3.0) - Option 2: Structured handling
use rs_pfcp::error::PfcpError;

match msg.unmarshal(data) {
    Ok(msg) => process(msg),
    Err(PfcpError::MissingMandatoryIe { ie_type, .. }) => {
        eprintln!("Missing required IE: {:?}", ie_type);
        // Can send appropriate PFCP error response
    }
    Err(PfcpError::IeParseError { ie_type, reason, .. }) => {
        eprintln!("Failed to parse {:?}: {}", ie_type, reason);
    }
    Err(e) => eprintln!("Other error: {}", e),
}
```

**Updating Constructors:**

```rust
// Before (v0.2.x)
let req = SessionEstablishmentRequest::new(seid_u64, seq_u32);

// After (v0.3.0)
use rs_pfcp::types::{Seid, SequenceNumber};
let req = SessionEstablishmentRequest::new(
    Seid::new(seid_u64),
    SequenceNumber::new(seq_u32)
);

// Or use From trait
let req = SessionEstablishmentRequest::new(
    seid_u64.into(),  // Implements From<u64> for Seid
    seq_u32.into()    // Implements From<u32> for SequenceNumber
);
```

**Updating Iterator Usage:**

```rust
// Before (v0.2.x) - deprecated methods (now removed)
// let pdr = msg.find_ie(IeType::CreatePdr);

// After (v0.3.0) - use iterator API
let pdr = msg.ies(IeType::CreatePdr).next();
```

---

## 🔄 Alternative: Incremental Approach

**If 10-14 days is too much at once:**

### v0.3.0-alpha.1: Error Type Only

- Implement PfcpError
- Leave newtype wrappers for later
- Timeline: 6-8 days
- Less breaking, faster release

### v0.3.0 Final: Add Newtypes

- Add newtype wrappers
- Remove deprecated methods
- Timeline: +2-3 days
- Complete the vision

**Benefit:** Faster iteration, more feedback cycles
**Drawback:** Two breaking change cycles instead of one

---

## 📚 Documentation Deliverables

1. **Migration Guide** (`docs/guides/v0.3.0-migration.md`)
   - Breaking changes list
   - Before/after code examples
   - Common migration patterns
   - Troubleshooting

2. **Error Handling Guide** (`docs/guides/error-handling.md`)
   - PfcpError variants explained
   - Error recovery patterns
   - Best practices
   - 3GPP Cause mapping

3. **Type Safety Guide** (`docs/guides/type-safety.md`)
   - Newtype wrapper benefits
   - Usage examples
   - Performance implications

4. **CHANGELOG.md**
   - Comprehensive breaking changes section
   - Migration instructions
   - New features

---

## 🚀 Next Steps

**All development work is complete. Remaining steps for release:**

1. ✅ **PfcpError migration** - COMPLETE
2. ✅ **Newtype wrappers** - COMPLETE (Seid, SequenceNumber, Teid in `src/types.rs`)
3. ✅ **Remove deprecated methods** - COMPLETE (find_ie, find_all_ies removed)
4. ✅ **Migration guide** - COMPLETE (`docs/guides/v0.3.0-migration.md`)
5. ✅ **CHANGELOG** - COMPLETE (v0.3.0 section written)
6. ✅ **Documentation updated** - All architecture and guide docs reflect v0.3.0 API
7. **Bump Cargo.toml version** to 0.3.0
8. **Run benchmarks** to confirm no performance regression
9. **Release candidate** - v0.3.0-rc.1 for community testing
10. **Final release** - v0.3.0 after feedback period

---

## 🤝 Stakeholder Review

**Completed Decisions:**

- [x] PfcpError implementation approach - Done in v0.2.x
- [x] Error variants design - 8 variants implemented
- [x] Migration strategy - Batched approach successful

**Decisions Made:**

1. ✅ **Naming:** `Seid`, `SequenceNumber`, `Teid` (concise, matching 3GPP terminology)
2. ✅ **Scope:** Priority newtype deferred to future release
3. ✅ All three newtypes implemented with Deref, From, Display

**Approval:**

- [x] PfcpError design approved (implemented)
- [x] Newtype wrapper design approved (implemented)
- [x] All v0.3.0 features implemented and tested

---

**Document Status:** Implementation Complete - Ready for Release
**Last Updated:** 2026-02-08
**Next Action:** Bump Cargo.toml version, run benchmarks, publish
